<% if @document.persisted? %>
  $('#upload_<%=params[:unique_id]%>').remove();
  $('#uploads_container').append("<%= j(render @document) %>");
<% else %>
  $('#upload_<%=params[:unique_id]%> div.progress').removeClass('active progress-striped').addClass('progress-danger');
<% end %>